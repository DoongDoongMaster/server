version: "3"

services:
  fastapi:
    container_name: fastapi
    build: .
    command: uvicorn app.main:app --host 0.0.0.0 --port 80 --reload
    ports:
      - 80:80
    volumes:
      - ./app:/code/app
